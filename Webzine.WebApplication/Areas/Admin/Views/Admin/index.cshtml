

    <div class="container">
        <div class="row">
            <h1>Artist @*Retrieve selected preview page*@ </h1>
            <hr />
            <button onclick="location.href='@Url.Action("", "createedit", new { area = "admin" })'" class="btn btn-primary btn-lg col-md-2 mb-3"><i class="fas fa-plus"></i> Add a new Artist@*Retrieve selected preview page*@</button>

            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        @*foreach*@
                        <th class="table-secondary" scope="col">#</th>
                        <th class="table-secondary" scope="col">First</th>
                        <th class="table-secondary" scope="col">Last</th>
                        <th class="text-center table-secondary" scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        @*foreach*@
                        <th scope="row">1</th>
                        <td>Mark</td>
                        <td>Otto</td>

                        <td class="text-center">
                            <a href='@Url.Action("", "createedit", new { area = "admin" })'> <i class="fas fa-edit"></i> </a>
                            <a href='@Url.Action("", "delete", new { area = "admin" })'><i class="fas fa-trash"></i></a>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>



